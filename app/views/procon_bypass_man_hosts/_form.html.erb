<%= form_with(model: @procon_bypass_man_host, local: true) do |form| %>
  <% if @procon_bypass_man_host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@procon_bypass_man_host.errors.count, "error") %> prohibited this procon_bypass_man_host from being saved:</h2>
      <ul>
        <% @procon_bypass_man_host.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :port %>
    <%= form.number_field :port %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
